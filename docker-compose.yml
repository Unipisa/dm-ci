version: '3.9'

services:
  app:
    image: ghcr.io/Unipisa/dm-ci
    ports:
      - "8000:8000"
    volumes:
      - /root/docker:/root/docker
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - PYTHONUNBUFFERED=1
